#include <stdio.h>
#include <stdlib.h>
#include <string.h>
int main()
{
    char sayi[10];
    char metin[100];
    printf("Sayi giriniz:");
    scanf("%s",sayi);
    char metinn[20] ;
    metinn[] = "Merhaba C." ;
    for(int i=0; i<strlen(sayi); i++)
    {
        if(sayi[i]=='0'){
            //metin[]="sýfýr ";
        }
       // else if(sayi[i]=='1'){
         //   metin= "bir ";
        }
       /* else if(sayi[i]==2)
            metin= {"iki "};
        else if(sayi[i]==3)
            metin= {"üç "};
        else if(sayi[i]==4)
            metin= {"dört "};
        else if(sayi[i]==1)
            metin= {"dört "};
        else if(sayi[i]==1)
            metin= {"beþ "};
        else  if(sayi[i]==1)
            metin= {"altý "};
        else  if(sayi[i]==1)
            metin= {"yedi "};
        else if(sayi[i]==1)
            metin= {"sekiz "};
        else if(sayi[i]==1)
            metin= {"dokuz "};*/
    }
//    printf("%s",metin);
//}
